import { colors } from 'mui/theme/colors';

const styles = {
  subtitle: {
    zIndex: 2,
    mt: '-16vh',
    mx: 'clamp(20px, 6.977vw, 200px)',
    fontSize: 'clamp(26px, 3.21vw, 56px)',
    lineHeight: '120%',
    color: colors.white,
    span: {
      color: colors.green
    }
  }
};

export default styles;
