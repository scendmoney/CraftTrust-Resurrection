import { colors } from 'mui/theme/colors';

const styles = {
  container: {
    margin: `0 0 103px 0`,
    padding: '0',
    overflowY: 'auto',
    display: 'flex',
    flexDirection: 'column-reverse'
  },
  button: { maxWidth: '100px', alignSelf: 'center', my: 2, border: `1px solid ${colors.gray4}` }
};

export default styles;
