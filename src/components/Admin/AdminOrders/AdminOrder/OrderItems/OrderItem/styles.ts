import { colors } from 'mui/theme/colors';

const styles = {
  container: {
    border: `1px solid ${colors.gray1}`,
    display: 'flex',
    borderRadius: '12px',
    alignItems: 'center',
    p: '20px',
    gap: 1
  }
};

export default styles;
