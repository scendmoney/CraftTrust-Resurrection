const styles = {
  block: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'flex-start',
    gap: '16px'
  },
  name: {
    display: 'flex',
    flexDirection: 'column',
    gap: 0.25
  },
  cardAction: { p: 0.5, borderRadius: '12px' }
};

export default styles;
