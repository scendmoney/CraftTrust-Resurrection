import { colors } from 'mui/theme/colors';

const styles = {
  menu: {
    color: colors.black,

    '& .MuiMenu-list': {},
    '& .MuiListItemText-primary': {
      color: colors.black,
      fontWeight: 500
    }
  }
};
export default styles;
