const styles = {
  wrapper: {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    gap: 3,
    minHeight: '100vh'
  },
  headerLine: {
    display: 'flex',
    gap: 1,
    alignItems: 'center',
    mb: 3,
    flexWrap: 'wrap',
    width: '100%'
  },
  notFound: {
    display: 'flex',
    justifyContent: 'center',
    mt: 'clamp(24px, 2.68vw, 240px)'
  }
};

export default styles;
