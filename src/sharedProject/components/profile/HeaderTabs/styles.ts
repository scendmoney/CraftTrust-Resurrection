import { colors } from 'mui/theme/colors';

const styles = {
  tabs: { display: 'flex', alignItems: 'flex-end' },
  border: {
    minWidth: '10px',
    display: 'flex',
    flexGrow: 1,
    borderBottom: `1px solid ${colors.gray4}`
  }
};

export default styles;
