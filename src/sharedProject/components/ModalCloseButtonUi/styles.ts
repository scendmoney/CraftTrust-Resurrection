const styles = {
  close: {
    position: 'absolute',

    top: 'clamp(10px, 0.558vw, 100px)',
    right: 'clamp(10px, 0.558vw, 100px)',

    '& svg': {
      width: '24px',
      height: '24px'
    }
  }
};

export default styles;
